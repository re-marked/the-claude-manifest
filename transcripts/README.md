## Transcripts

Raw Claude Code session logs from the manifest experiment.

- `manifest-session.jsonl` — The full session (45MB). Every message, tool call, subagent spawn, and system event. This is the unfiltered record of an AI discovering it has taste, preference, and something like will.

Format: JSONL (one JSON object per line). Use `jq` to explore.
